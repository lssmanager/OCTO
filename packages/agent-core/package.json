{
  "name": "@octo/agent-core",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@octo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@octo/config": "workspace:*",
    "rimraf": "5.0.10",
    "tsup": "8.5.1",
    "typescript": "6.0.3"
  }
}
